# Flask Application Core
SECRET_KEY='your_very_secret_flask_key_here_please_change_me'
FLASK_APP='wsgi:app' # Or app:app, depending on your flask run setup
FLASK_ENV='development' # Should be 'production' in production
FLASK_CONFIG='development' # Should be 'production' in production

# Database
# For local development with init_db.py, leave DATABASE_URL unset to use C:\temp_db\sales_training_temp.db
# For production (e.g., Heroku Postgres), this will be set by the hosting environment:
# DATABASE_URL='postgresql://user:password@host:port/dbname'

# API Keys - Replace with your actual keys or leave blank if not used
ANTHROPIC_API_KEY=''
OPENAI_API_KEY=''
ELEVEN_LABS_API_KEY='' # or ELEVENLABS_API_KEY
DEEPGRAM_API_KEY=''

# Rate Limiting (Optional, defaults to in-memory)
# REDIS_URL='redis://localhost:6379/0'

# Google OAuth (Optional)
# GOOGLE_CLIENT_ID=''
# GOOGLE_CLIENT_SECRET=''

# Base URL for callbacks etc.
# BASE_URL='http://127.0.0.1:5000' # For local
# BASE_URL='https://your-production-domain.com' # For production

# Admin User for init_db.py (Optional, if you use the seeding feature in init_db.py)
# ADMIN_EMAIL='admin@example.com'
# ADMIN_PASSWORD='strongpassword'