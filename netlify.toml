[build]
  base    = "app/frontend/"  # Path to your frontend project
  publish = "dist/"          # Publish directory (relative to 'base')
  command = "npm run build"  # Your build command

# Optional: You can also specify the Node.js version here if needed,
# though your UI settings already show Node.js 22.x which should be fine.
# [build.environment]
#   NODE_VERSION = "22" 